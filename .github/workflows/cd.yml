name: CD

on:
  push:
    branches:
      - main

# env:
#   REGISTRY: ghcr.io
#   API_IMAGE: ghcr.io/${{ github.repository_owner }}/bible-reader-api
#   WEB_IMAGE: ghcr.io/${{ github.repository_owner }}/bible-reader-web

jobs:
  # build-and-push:
  #   name: Build and push Docker images
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Build and push API image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: apps/api/Dockerfile
  #         push: true
  #         tags: ${{ env.API_IMAGE }}:${{ github.sha }},${{ env.API_IMAGE }}:latest
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  #     - name: Build and push web image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: apps/web/Dockerfile
  #         push: true
  #         tags: ${{ env.WEB_IMAGE }}:${{ github.sha }},${{ env.WEB_IMAGE }}:latest
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max

  deploy-api:
    name: Deploy API to Render
    runs-on: ubuntu-latest
    # needs: build-and-push

    steps:
      - name: Trigger Render deploy
        run: |
          curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }} \
            --fail \
            --silent \
            --show-error

  deploy-web:
    name: Deploy web to Vercel
    runs-on: ubuntu-latest
    # needs: build-and-push

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Deploy to Vercel
        run: vercel deploy --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}